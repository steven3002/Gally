//! Rust structs mapping to DB rows.
//!
//! BI-M1 only exercises the cursor and `raw_events` write paths (see `queries.rs`); typed
//! row models are added per-feed in BI-M2..BI-M4 as their tables land.
