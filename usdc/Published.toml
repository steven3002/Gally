# Generated by Move
# This file contains metadata about published versions of this package in different environments
# This file SHOULD be committed to source control
#
# PRODUCTION (mainnet): the `usdc` package IS Circle's already-deployed USDC. We do NOT
# publish this package on mainnet — this entry points the dependency at Circle's canonical
# package id so any consumer (gally_core, gally_mock_faucet) built with `--build-env
# mainnet` references the REAL `USDC` coin (verified on-chain: 6-decimal, symbol "USDC").
#
# localnet has NO entry here on purpose: `run_stack.sh` publishes the locally-mintable mock
# to the live-sim node and writes the fresh `[published.localnet]` itself each run (it is an
# ephemeral, per-chain record — not committed).

[published.mainnet]
chain-id = "35834a8a"
published-at = "0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7"
original-id = "0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7"
version = 1
toolchain-version = "1.73.1"
build-config = { flavor = "sui", edition = "2024" }
